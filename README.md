# 🔐 MTA Login Panel (Beta)

**Логин-панель для MTA:SA** — система авторизации с сохранением данных, проверкой кодового слова и защитой от мультиаккаунтинга.

> ⚠️ **Бета-версия** : некоторые функции находятся в разработке

## Основные возможности

* ✅ Авторизация с запоминанием данных
* ✅ Проверка кодового слова для новых устройств
* ✅ Ограничение создания нескольких аккаунтов с одного ПК
* ✅ Валидация вводимых данных

## Ограничения (Beta)

* ⚠️ **Отсутствует автоматический кик** при превышении лимита попыток
* ⚠️ **Нет шифрования** файла с данными авторизации на клиенте

## Быстрая установка

1. Скачайте архив с ресурсом
2. Поместите папку `login-panel` в `resources/`
3. Добавьте в `mtaserver.conf`:

**xml**

```
<resource src="login-panel" startup="1" />
```

4. Выдать права для панели через команду `aclrequest list login-panel allow`
5. Перезапустите сервер

## Настройки

**xml**

```
<settings>
    <setting name='@SignInAttemptsLimit' value='5' desc="Лимит попыток авторизации"/>
    <setting name='@Check2FAAttemptsLimit' value='5' desc="Лимит попыток проверки кодового слова" />
</settings>
```

## Предупреждение

Данная бета-версия предназначена для тестирования и отладки. Файл с данными авторизации (`authData.enc`) хранится на клиенте **без шифрования** — не рекомендуется использование для "боевой-среды".

`Минимальная версия клиента и сервера MTA:SA 1.6.0-9.22430.0`
